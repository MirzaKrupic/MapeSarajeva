<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mirza.mapesarajeva">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsSkakavac"
            android:label="@string/title_activity_maps_skakavac" />
        <activity
            android:name=".MapsLcuprija"
            android:label="@string/title_activity_maps_lcuprija" />
        <activity
            android:name=".MapsVijecnica"
            android:label="@string/title_activity_maps_vijecnica" />
        <activity
            android:name=".MapsBtabija"
            android:label="@string/title_activity_maps_btabija" />
        <activity
            android:name=".MapsZtabija"
            android:label="@string/title_activity_maps_ztabija" />
        <activity android:name=".RezervacijaActivity" />
        <activity
            android:name=".MapsCarsija"
            android:label="@string/title_activity_maps_carsija" />
        <activity android:name=".casijamap" />
        <activity android:name=".btabija" />
        <activity android:name=".ztabija" />
        <activity android:name=".skakavac" />
        <activity android:name=".vijecnica" />
        <activity android:name=".latinc" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registracija" />
        <activity
            android:name=".MainMenu"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".carsija" />
    </application>

</manifest>